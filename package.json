{
  "name": "copilot-usage-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @promptstreak/web dev",
    "build:web": "pnpm --filter @promptstreak/web build",
    "build:schema": "pnpm --filter @copilot-usage/shared-schema build",
    "lint:web": "pnpm --filter @promptstreak/web lint",
    "db:push": "pnpm --filter @promptstreak/web db:push",
    "db:migrate": "pnpm --filter @promptstreak/web db:migrate",
    "db:generate": "pnpm --filter @promptstreak/web db:generate"
  }
}
