/** Single switch that gates the entire Cost Estimator feature.
 *  Set to `false` to hide the dashboard button + skip command registration. */
export const enableCostEstimator = true;

/** Status badge shown in the panel header. */
export const costEstimatorStatus: 'preview' | 'beta' | 'ga' = 'preview';
